// This component is to check whether the user is logged in, 
//     if yes, display the dashboard
//     if no, display the log in page 
//  The useEffect will be used to watch on the auth state changes, to render appropriate components
const Dashboard = () => {
    return (
        <div>Hello</div>
    )
}

export default Dashboard